alias: heating_delta_plus_input_number
sequence:
  - action: input_number.set_value
    target:
      entity_id: "{{ entity }}"
    data:
      value: >
        {% set current = states(entity) | float(0) %} {{ [current + 1, 5] | min
        }}
description: ""
